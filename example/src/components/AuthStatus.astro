---
// Header auth widget built from the SDK's control + button components — the
// SignedIn/SignedOut branches are decided server-side, so the other branch
// never reaches the browser.
import { SignedIn, SignedOut, SignInButton, SignUpButton, UserButton } from '@workos/authkit-astro/components';
---

<div class="auth-status">
  <SignedIn>
    <UserButton />
  </SignedIn>
  <SignedOut>
    <SignInButton />
    <SignUpButton />
  </SignedOut>
</div>
